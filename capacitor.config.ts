import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.pokeInfoDex',
  appName: 'PokeInfoDex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
